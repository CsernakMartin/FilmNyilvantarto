<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FilmNyilvantarto.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FilmNyilvantarto"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FilmNyilvantarto">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Spacing="10" Margin="0,0,0,20">
            <TextBox x:Name="SearchTextBox" 
                     Width="200" 
                     PlaceholderText="Keresés..." 
                     TextChanged="OnSearchTextChanged"/>

            <Button x:Name="SearchButton" 
                    Click="OnAddButtonClick"
                    Content="+" 
                    FontSize="20"
                    Width="40"
                    Height="40"
                    Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
        <ListView x:Name="MovieListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Movie">
                    <Grid Margin="0,5" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="16" FontWeight="Bold">
                            <Run Text="{x:Bind Title}"/>
                            <Run Text=" - "/>
                            <Run Text="{x:Bind ReleaseYear}"/>  
                        </TextBlock>

                        <Button Grid.Column="1" 
                        Content="&#xE74D;" 
                        FontFamily="Segoe MDL2 Assets"
                        Foreground="Red"
                        Background="Transparent"
                        Click="OnDeleteButtonClick" 
                        VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    
</Window>
